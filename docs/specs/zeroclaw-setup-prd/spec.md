# ZeroClaw Setup PRD Mirror

Canonical product requirements currently live in the repository root at [PRD.md](../../../PRD.md).

This mirrored spec entrypoint exists so the repository structure matches neighboring GonkaGate setup utilities that keep specs under `docs/specs/`.

Until the spec is split into smaller documents, treat `PRD.md` as the single source of truth.
